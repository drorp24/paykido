/*
    $('.purchaseButton').click(function() {

        purchase_id = $(this).attr("id").split('_')[1];
        approved = $(this).hasClass('approve').toString();
        url = 'control/purchase/' + purchase_id + '?approved=' + approved
        hs.htmlExpand(null, { 
                    src: url, objectType: 'ajax', 
                    creditsPosition: 'bottom left', 
                    wrapperClassName: 'titlebar' } )
    });
--> 
