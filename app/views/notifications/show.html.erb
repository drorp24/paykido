<%= render 'primary_navigation'  %>
<%= render 'consumer_navigation' %>

<section id="maincontainer">

	<%= render 'page_head', :page_title => 'Notification Details', :backPath => consumer_purchase_path(@consumer, @purchase) %>
	
	<table>
		<tbody>
			<% @notification.attributes.each do |name, value| %>
			  <% next if name == 'id' or !value %>
			  <tr>
			    <td><%= name %></td>
			    <td><%= value %></td>
			  </tr>
			<% end %>
		</tbody>
	</table>

	<br />

</section>
