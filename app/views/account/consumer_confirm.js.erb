<% if @error %>
    notification('<%= @error %>', true, 'user', true)
<% else %>
	$('.consumer.active').addClass('confirmed');
    notification("<%= "#{@consumer.name} confirmed by #{@payer.name}"%>", false, 'user', true)
<% end %>
